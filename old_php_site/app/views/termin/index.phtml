<?php // TODO: find good date picker and implement id with AJAX ?>
<?php // TODO: consider usnig Alpine.js ?>
<h1>Termin buchen</h1>

<p><?= $this->view->behandlung ?></p>

<pre>
  <?php foreach ($this->view->example as $key => $value) { ?>

  <?= $key ?>: <?= gettype($value) == 'array' ? '['.implode(',', $value).']' : $value ?>

  <?php } ?>
</pre>
